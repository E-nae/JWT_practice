import styles from './LoginLoader.module.css';

// ==============================|| Loader ||============================== //

const LoginLoader = () => (
  <div className={styles.loaders}>
    ONE<span className={styles.accent}>4</span>U
  </div>
);

export default LoginLoader;

